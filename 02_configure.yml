---

- name: "Step 2: Configure the rest"
  hosts: 127.0.0.1
  connection: local
  roles:
    # from bootstrap
    - git-configuration
    - ssh-configuration
    - shell-configuration
    - custom-scripts
    # other
    - nvim-configuration
    - polybar-configuration
    - gopass-configuration
    - i3-configuration
