# This is Git's per-user configuration file.
[user]
    name = Christian Heusel
    email = christian@heusel.eu
    signkey = christian@heusel.eu
[core]
    editor = nvim
    excludesFile = ~/.gitignore
    quotePath = false
    commitGraph = true
[merge]
    tool = nvim -d
[gpg]
    programm = gpg
[commit]
    gpgsign = true
[pull]
    rebase = true
[push]
    default = current
[init]
    defaultBranch = main
[receive]
    advertisePushOptions = true
[gc]
    writeCommitGraph = true
[sendemail]
; setup for using git send-email; prompts for password
smtpuser = christian@heusel.eu
smtpserver = smtp.1und1.de
smtpencryption = tls
smtpserverport = 587
[user]
    email = christian@heusel.eu
    name = Christian Heusel
[alias]
    # git reapply origin/git-packages
    reapply = "!f() { local head=$(git rev-parse HEAD) && local base=$(git rev-parse \"${2:-HEAD}\") && git reset --hard \"$1\" && git cherry-pick \"$base^..$head\"; }; f"
    jedi = push --force-with-lease
    grepa = "!f() { git rev-list --all | xargs git grep -I --line-number \"$@\"; }; f"
    grab = "!f() { set -euo pipefail; url=$(git remote get-url origin); git fetch \"${url/:*}:$1/${url//*\\/}\" \"$2\"; git switch --create \"$2\" FETCH_HEAD; git branch | --set-upstream-to=${3:-origin/$(git default-branch)};  }; f"
